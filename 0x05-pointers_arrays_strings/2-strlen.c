#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: char
 * return: int
 */
void _strlen(char *s)
int _strlen(char *s);
{
int i;
for (i = o; s[i] != '\0'; i++)
{
continue;
}
return (i);
}
