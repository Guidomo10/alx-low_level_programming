#include "main.h"

/**
 * main - prints the name of the programm
 * @argc: integger.
 * @argv: string.
 *
 * Return: integger.
 */

int main(int argc, char *argv[])
{
if (argc == 1)
{
printf("%s\n", argv[0]);
}
return (0);
}
